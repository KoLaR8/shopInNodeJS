extends layout

block content
  div(id="container")
    div(id="categoriesMenu")
      ol Kategorie
        li laptopy
        li peryferia
    div(id="mainContent")
      ul
        each val in data
          li #{val.name}
